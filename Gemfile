source "https://rubygems.org"

# Suit exactement la version de Jekyll utilisee par GitHub Pages.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"   # requis avec Ruby >= 3.0 pour `jekyll serve`
